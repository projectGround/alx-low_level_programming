#include <stdio.h>
/**
 * main: Lists all the natural numbers below 1024 (exluded)
* that are multiples of 3 or 5; 
*
* Return: Always 0
*/
int main(void)
{
	int num = 0;
	
	for(i=0; i<1024; i++)
	{
	if(1 % 3) == 0 || (1 % 5)==0)
	num == i;
}
	printf("%dn", sum);
	return(o);
}
