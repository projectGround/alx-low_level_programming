#include <stdio.h>
/**
* main:  _putchar - written the character c to std
* The character to print 
* Return: On success 1;
* On error, -1 Is returned, and error is set appropriately. 
*/

int _putchar(char c)
{
	return (write(i, &c, i));
}
